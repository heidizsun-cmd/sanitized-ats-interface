{
  "name": "sanitized-ats-interface",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static browser-local ATS portfolio demo with fictional data and explainable matching.",
  "scripts": {
    "dev": "npx serve . -l 4174",
    "test": "node --test",
    "check": "node --check app.js && node --check demo-logic.js && node --test"
  }
}
