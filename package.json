{
  "name": "sanitized-ats-interface",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first ATS portfolio prototype with a careers page, recruiter dashboard, and resume parsing.",
  "scripts": {
    "preview": "python3 server.py",
    "check": "node --check app.js && python3 -m unittest -v"
  }
}
